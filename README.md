# Logging-configs
可以直接使用的python logging配置，支持文件写入，控制台输出，文件轮转。
# python-logging-config

开箱即用的 Python logging 配置。

## 特性
- 控制台输出
- 文件按大小/日期轮转
- 支持通过环境变量调整日志级别
- 可直接复制到项目中使用

## 使用
1. 复制 `log_config.py` 或 `logging.conf` 到你的项目
2. 在入口文件导入并初始化
3. 按需修改日志路径和级别
